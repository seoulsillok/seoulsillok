---
import Layout from '../../layouts/Layout.astro';
import SeoulArtMap from '../../components/SeoulArtMap.astro';
import { ALL_DONGS, DISTRICTS } from '../../data/seoul';
---

<Layout title="서울 동네 아카이브" description="서울 25개 구 동네 목록과 지도 탐색">
  <section class="intro">
    <p class="eyebrow">SEOUL DONG DIRECTORY</p>
    <h1 data-copy-locale="ko">서울 동네 기록</h1>
    <h1 data-copy-locale="en">Seoul Neighborhood Directory</h1>
    <p data-copy-locale="ko">
      지도 위 구 라벨을 클릭하면 해당 구만 확대되고,
      그 구에 속한 동 이름이 실제 영역 위에 표시되며 패널에서도 바로 각 동 페이지로 이동할 수 있습니다.
    </p>
    <p data-copy-locale="en">
      Click a district label on the map to zoom into that district only.
      Dong names appear over their actual regions, and you can also open each dong page from the panel.
    </p>
  </section>

  <section class="map-area">
    <SeoulArtMap districts={DISTRICTS} mode="interactive" />
  </section>

  <section class="status-bar" aria-live="polite">
    <p><strong data-selected-label></strong> · <span data-visible-copy data-total={ALL_DONGS.length}></span></p>
  </section>

  <section class="district-grid">
    {DISTRICTS.map((district) => (
      <article class="district-card" id={`district-${district.code}`} data-district-card data-district-code={district.code} data-dongs={district.dongs.length}>
        <header>
          <h2>{district.nameKo}</h2>
          <small>
            <span data-copy-locale="ko">{district.dongs.length}개 동네</span>
            <span data-copy-locale="en">{district.dongs.length} dongs</span>
          </small>
        </header>
        <ul>
          {district.dongs.map((dong, index) => (
            <li>
              <a href={`/dong/${district.code}-${index + 1}`}>
                <strong>{dong}</strong>
                <span data-copy-locale="ko">기록 보기</span>
                <span data-copy-locale="en">View record</span>
              </a>
            </li>
          ))}
        </ul>
      </article>
    ))}
  </section>
</Layout>

<script>
  const cards = Array.from(document.querySelectorAll('[data-district-card]'));
  const selectedLabel = document.querySelector('[data-selected-label]');
  const visibleCopy = document.querySelector('[data-visible-copy]');

  const districtNameByCode = new Map(
    cards.map((card) => [
      card.dataset.districtCode,
      card.querySelector('h2')?.textContent?.trim() || ''
    ])
  );

  let activeCode = '';

  function getLocale() {
    return document.documentElement.dataset.siteLocale === 'en' ? 'en' : 'ko';
  }

  function formatVisibleCopy(count) {
    return getLocale() === 'en' ? `${count} dongs shown` : `${count}개 동네 표시 중`;
  }

  function applySelection(code) {
    const normalized = code || '';
    activeCode = normalized;

    let count = 0;
    cards.forEach((card) => {
      const isMatch = !normalized || card.dataset.districtCode === normalized;
      card.hidden = !isMatch;
      if (isMatch) {
        count += Number(card.dataset.dongs || 0);
      }
    });

    if (selectedLabel) {
      selectedLabel.textContent = normalized ? districtNameByCode.get(normalized) || (getLocale() === 'en' ? 'Selected' : '선택') : getLocale() === 'en' ? 'All' : '전체';
    }

    if (visibleCopy) {
      const total = Number(visibleCopy.dataset.total || 0);
      visibleCopy.textContent = formatVisibleCopy(count || total);
    }
  }

  window.addEventListener('district-change', (event) => {
    const code = event?.detail?.code || '';
    applySelection(code);
  });

  window.addEventListener('site-locale-change', () => {
    applySelection(activeCode);
  });

  const initialCode = location.hash.startsWith('#district-') ? location.hash.replace('#district-', '') : '';
  const isValid = cards.some((card) => card.dataset.districtCode === initialCode);
  applySelection(isValid ? initialCode : '');
</script>

<style>
  .intro {
    margin-top: 0.2rem;
  }

  .eyebrow {
    margin: 0;
    letter-spacing: 0.12em;
    color: #787064;
    font-weight: 700;
    font-size: 0.72rem;
  }

  h1 {
    margin: 0.6rem 0 0.45rem;
    font-family: 'Fraunces', serif;
    font-size: clamp(1.9rem, 3vw, 2.6rem);
    color: #1f1d18;
  }

  .intro p {
    color: #4d463d;
    max-width: 760px;
    line-height: 1.7;
  }

  .map-area {
    margin-top: 1rem;
  }

  .status-bar {
    margin-top: 0.85rem;
    color: #4d463d;
    font-size: 0.92rem;
  }

  .status-bar p {
    margin: 0;
  }

  .district-grid {
    margin-top: 1rem;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .district-card {
    border: 1px solid #e4dfd4;
    border-radius: 16px;
    padding: 0.95rem;
    background: #fffdf9;
  }

  .district-card header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 0.8rem;
    margin-bottom: 0.75rem;
  }

  h2 {
    margin: 0;
    color: #1f1d18;
    font-size: 1.15rem;
  }

  header small {
    color: #6a6258;
  }

  ul {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.42rem;
  }

  li a {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.45rem;
    border: 1px solid #e7e1d6;
    border-radius: 10px;
    text-decoration: none;
    padding: 0.44rem 0.56rem;
    color: #24211c;
    font-size: 0.84rem;
    background: #ffffff;
  }

  li a:hover {
    border-color: #1f1d19;
  }

  li span {
    color: #70685d;
    font-size: 0.72rem;
  }

  @media (max-width: 960px) {
    .district-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    ul {
      grid-template-columns: 1fr;
    }
  }
</style>
